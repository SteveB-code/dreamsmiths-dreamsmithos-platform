import { Header } from "@/components/layout/header";

export default function PlatformsPage() {
  return (
    <div>
      <Header title="Platforms" />
      <div className="p-6">
        <p className="text-muted-foreground">
          Platform Registry — coming in the next build session.
        </p>
      </div>
    </div>
  );
}
